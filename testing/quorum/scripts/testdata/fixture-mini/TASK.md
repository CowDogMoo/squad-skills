# Task: sumrange

Implement `SumRange(lo, hi int) int` returning the sum of every integer from
`lo` through `hi` inclusive.

Acceptance criteria:

1. `SumRange(1, 3)` returns 6.
2. `SumRange(lo, hi)` with `lo > hi` returns 0.
3. Every criterion above has a covering test in `main_test.go`.
